import { merida } from './dicts';

function meridatofen(input: string) {
  var top = `!""""""""#\n`;
  var bottom = '/(((((((()';

  for (let line = 8; line >= 1; line--) {
    var init = '$';
    for (let column = 0; column < 8; column++) {
      if (line % 2 === 0) {
      }
    }
  }
}
